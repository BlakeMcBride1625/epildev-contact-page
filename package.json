{
  "name": "epildev-contact-page",
  "version": "1.0.0",
  "description": "Quantum-inspired futuristic contact page for EpilDev",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && VITE_LOCALHOST_FE=${localhost_fe:-1001} npm run dev",
    "dev:backend": "cd backend && LOCALHOST_BE=${localhost_be:-1002} LOCALHOST_FE=${localhost_fe:-1001} npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}


