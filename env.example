# Server Configuration
PORT=200
NODE_ENV=development
FRONTEND_URL=http://localhost:100

# ========================================
# DUAL FRONTEND CONFIGURATION
# ========================================
# 
# 🚀 EASY FRONTEND URL CHANGE (5 SIMPLE STEPS):
#   1. Open this file → Find the FRONTEND_DOMAIN lines below
#   2. Copy your new domains → Get the domains you want to use
#   3. Replace the values → Change the domains after the = signs
#   4. Save the file → Ctrl+S or Cmd+S to save changes
#   5. Restart the app → Run 'npm run dev' or restart Docker
#
# 📝 EXAMPLES:
#   FRONTEND_DOMAIN_1=8bp.epildevconnect.uk
#   FRONTEND_DOMAIN_2=8bp.management.epildevconnect.uk
#   FRONTEND_DOMAIN_1=my-website.com
#   FRONTEND_DOMAIN_2=admin.my-website.com
#
# ========================================
FRONTEND_DOMAIN_1=8bp.epildevconnect.uk
FRONTEND_DOMAIN_2=8bp.management.epildevconnect.uk

# ========================================
# API CONFIGURATION
# ========================================
# 
# 🚀 EASY API URL CHANGE (5 SIMPLE STEPS):
#   1. Open this file → Find the API_ENDPOINTS line below
#   2. Copy your new URL → Get the API domain/path you want to use
#   3. Replace the value → Change the URL after the = sign
#   4. Save the file → Ctrl+S or Cmd+S to save changes
#   5. Restart the app → Run 'npm run dev' or restart Docker
#
# 📝 EXAMPLES:
#   API_ENDPOINTS=api.config-8bp.epildevconnect.uk/point
#   API_ENDPOINTS=api.epildevconnect.uk/contact  
#   API_ENDPOINTS=my-api.example.com/api
#   API_ENDPOINTS=backend.mysite.com/endpoint
#
# ========================================
API_ENDPOINTS=api.config-8bp.epildevconnect.uk/point

# Local Development Ports
localhost_fe=100
localhost_be=200

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
ADMIN_EMAIL=admin@epildevconnect.uk

# Security
JWT_SECRET=your-super-secret-jwt-key-here
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=5

# Database (if using one in the future)
# DATABASE_URL=postgresql://user:password@localhost:5432/epildev_contact

# Cloudflare (for production)
# CLOUDFLARE_API_TOKEN=your-cloudflare-token
# CLOUDFLARE_ZONE_ID=your-zone-id


